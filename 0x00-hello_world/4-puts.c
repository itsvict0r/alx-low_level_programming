#include <stdio.h>

/**
  * main - entry point to a C program that prints a line with
  * puts() function as opposed to printf()
  *
  * Return: 0 (Success)
  */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
